/// Endpoint paths for the settings feature.
class SettingsEndpoints {
  SettingsEndpoints._();

  /// GET — fetch the signed-in user's settings.
  /// PUT — update the signed-in user's settings.
  static const String settings = '/settings';
}
